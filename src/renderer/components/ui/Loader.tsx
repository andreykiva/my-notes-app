const Loader = () => {
	return (
		<div
			data-testid="loader"
			className="mx-auto my-24 border-t-4 border-white border-solid w-16 h-16 rounded-full animate-spin"
		></div>
	);
};

export default Loader;
